# macOS
.DS_Store

# Xcode
build/
# Ad-hoc `xcodebuild -derivedDataPath build-test` runs. Sibling of build/,
# and it got swept into a commit by a `git add -A` once already.
build-test/
DerivedData/
*.xcuserdata/
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/

# Swift Package Manager
.build/
Packages/
Package.resolved
*.xcodeproj

# CocoaPods (if used)
Pods/

# Carthage
Carthage/Build

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots

# Python (for bundled spotify-downloader during dev)
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# Secrets / local config
*.secrets
.env
.env.local

# Claude Code session cache
.claude/

# Chat attachment uploads — land in the working tree and can contain
# screenshots of secrets. This repo is public; never let these in.
.roder-uploads/
